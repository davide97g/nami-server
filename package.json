{
  "name": "nami",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "deploy": "turbo run deploy"
  },
  "devDependencies": {
    "turbo": "^2.6.1",
    "@types/bun": "latest"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "bun@1.3.3",
  "module": "index.ts",
  "type": "module",
  "peerDependencies": {
    "typescript": "^5.9.3"
  }
}
